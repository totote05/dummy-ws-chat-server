{
  "name": "dummy-ws-chat-server",
  "version": "1.0.0",
  "description": "Dummy WS chat server",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d dist",
    "start": "npm run build && node dist",
    "restart": "rimraf dist && npm run start",
    "dev": "nodemon --exec npm run restart"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "web-push": "^3.4.4",
    "websocket": "^1.0.34"
  },
  "devDependencies": {
    "@babel/cli": "^7.13.16",
    "@babel/core": "^7.14.0",
    "@babel/preset-env": "^7.14.1",
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2"
  }
}
